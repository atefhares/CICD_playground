node {
  stage('build infrastructure using terraform') {
         sh 'ls -la /work_dir'
         sh 'cp /work_dir/credentials ~/.aws/credentials'
         sh 'terraform init /work_dir'
         sh 'terraform apply -var-file /work_dir/devel.tfvars /work_dir -auto-approve'
   }
}