pipeline {
    agent any
    stages {
        stage('test') {
            steps {
               sh "python tests/test.py"
            }
        }

        // stage('Build & Deploy') {
        //     steps {
        //         sh "docker-compose up "
        //     }
        // }
    }
}